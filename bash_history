whoami
hostname
id
uname -a
ifconfig
sudo -l
git clone https://github.com/python/cpython
mysql -u root -p
mysql -u root -proot
mysql -u root -proot mysql
mysqldump -u root -proot -A > /tmp/db.backup
sudo su -
mkdir projs
cd projs
git clone https://github.com/python/cpython
git clone git@github.com:django/django.git
