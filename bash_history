whoami
hostname
id
uname -a
ifconfig
sudo -l
git clone https://github.com/python/cpython
mysql -h db -u root -p
mysql -h db -u root -proot
mysql -h db -u root -proot mysql
mysqldump -h db -u root -proot -A > /tmp/db.backup
sudo su -
mkdir projs
cd projs
git clone https://github.com/django/django
